interface:
  display_name: "Investigate + Implement"
  short_description: "Investigate a coding task, choose a direction, implement it, verify it, open or update a PR, and clean up after merge."
  default_prompt: "Understand the task intent, start a dedicated worktree, investigate the current state thoroughly, give a ranked list if needed, choose the best option, implement it, verify it, open or update the PR, and clean up the worktree after the PR is merged."
